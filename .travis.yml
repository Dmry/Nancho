language: cpp

matrix:
  include:
    - dist: bionic
      addons:
        apt:
          packages:
            - g++-7
            - libdbus-1-dev
            - libpulse-dev
      env:
        - MATRIX_EVAL="CC=gcc-7 && CXX=g++-7"

before_script:
  - mkdir build
  - cd build
  - cmake ..
  - eval "${MATRIX_EVAL}"

script:
  - make
      

